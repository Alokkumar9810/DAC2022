#include<math.h>
double GetIntrest(double p, float r, int n)
{
return (p*r*n)/100;
}
double GetCompoundIntrest(double p,float r, int n)
{
double amount=0;
amount=p*pow((1+r/100),n);
return amount-p;
}























































































































